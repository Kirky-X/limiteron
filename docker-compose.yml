version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: limiteron
      POSTGRES_PASSWORD: limiteron_dev
      POSTGRES_DB: limiteron_test
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U limiteron -d limiteron_test"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
