[package]
name = "limiteron-examples"
version = "0.1.0"
edition = "2021"
description = "Limiteron examples - demonstrating all features"

[dependencies]
limiteron = { path = "..", features = ["full"] }
futures = "0.3"

[dev-dependencies]
tokio = { version = "1.35", features = ["full"] }
